export interface UserInfoInterface {
  email: string;
  exp: number;
  iat: number;
  maxEpx: number;
  name: string;
  role: string | null;
  selectedFileId: string | null;
  userId: string;
  userName: string;
}
